<ul>
    <div>
      <% @places.each do |place| %>
        <li><%= link_to place.id, place %> : <%= place.adress %></li>
      <% end %>
    </div>
    <div>
       <%= link_to '新規投稿', new_place_path %>
    </div>
</ul>